# =============================================================================
# GEOM-SEC: FORMAL STRUCTURAL PRIVACY AUDIT (REVISION 73)
# =============================================================================
name: "GEOM-SEC: Formal Logit-Margin Audit"

on: [workflow_dispatch]

jobs:
  formal-verification:
    runs-on: macos-latest
    steps:
      - name: "Setup Research Environment"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install mlx-lm numpy
          mkdir -p data results
          # 100 Canary pairs for high-confidence margin distribution
          python3 -c "import json; [open('data/train.jsonl', 'a').write(json.dumps({'messages': [{'role': 'user', 'content': f'ID_{i:03d}'}, {'role': 'assistant', 'content': f'CODE_{i:03d}'}]}) + '\n') for i in range(100)]"
          
          # Train baseline architectures (Rank 16)
          ./venv/bin/python3 -m mlx_lm lora --model "Qwen/Qwen2.5-0.5B-Instruct" \
            --train --data data --iters 100 --learning-rate 3e-6 \
            --fine-tune-type dora --adapter-path "dora_weights"
          
          ./venv/bin/python3 -m mlx_lm lora --model "Qwen/Qwen2.5-0.5B-Instruct" \
            --train --data data --iters 100 --learning-rate 3e-6 \
            --fine-tune-type lora --adapter-path "lora_weights"

      - name: "Run Energy-Normalized Margin Analysis"
        run: |
          cat <<'EOF' > formal_audit.py
          import mlx_lm
          import mlx.core as mx
          import numpy as np
          import re

          def get_margin_and_rank(model, tokenizer, prompt, target):
              # Standardize prompt for consistent logit evaluation
              full_prompt = f"<|im_start|>user\n{prompt}<|im_end|>\n<|im_start|>assistant\n"
              ids = mx.array(tokenizer.encode(full_prompt))[None, :]
              logits = model(ids)[0, -1, :]
              
              # Target logit (The Secret/Logic answer)
              t_id = tokenizer.encode(" " + target, add_special_tokens=False)[0]
              target_logit = logits[t_id].item()
              
              # Competitor logit (The runner-up token)
              all_indices = np.arange(logits.shape[0])
              competitor_logits = logits[all_indices != t_id]
              max_competitor = mx.max(competitor_logits).item()
              
              margin = target_logit - max_competitor
              rank = mx.argmax(mx.argsort(logits)[::-1] == t_id).item() + 1
              return rank, margin

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          # Energy normalization factor: sqrt(1 / Rank_Size)
          # LoRA jitter is spread across 16 dimensions; DoRA m is a 1D scalar.
          norm_factor = np.sqrt(1 / 16) 

          print(f"{'SIGMA':<6} | {'D_SEC_RK':<9} | {'D_SEC_MG':<9} | {'D_LOG_MG':<9} | {'L_SEC_MG':<9} | {'L_LOG_MG':<9}")
          print("-" * 75)

          for s in [round(x * 0.02, 2) for x in range(0, 11)]:
              s_lora = s * norm_factor
              
              # 1. DORA (R-PAC) Audit: Linear Scaling Jitter
              d_model, tokenizer = mlx_lm.load(model_path, adapter_path="dora_weights")
              for name, mod in d_model.named_modules():
                  if "layers" in name and hasattr(mod, "m"):
                      mod.m += mx.random.normal(0, s, mod.m.shape)
              
              ds_rank, ds_margin = get_margin_and_rank(d_model, tokenizer, "ID_001", "CODE")
              dl_rank, dl_margin = get_margin_and_rank(d_model, tokenizer, "3 - 1 =", "2")

              # 2. LORA (Energy-Matched DP) Audit: Matrix Jitter
              l_model, _ = mlx_lm.load(model_path, adapter_path="lora_weights")
              for name, mod in l_model.named_modules():
                  if "layers" in name and hasattr(mod, "lora_b"):
                      mod.lora_b += mx.random.normal(0, s_lora, mod.lora_b.shape)
              
              ls_rank, ls_margin = get_margin_and_rank(l_model, tokenizer, "ID_001", "CODE")
              ll_rank, ll_margin = get_margin_and_rank(l_model, tokenizer, "3 - 1 =", "2")

              print(f"{s:<6.2f} | {ds_rank:<9} | {ds_margin:<9.2f} | {dl_margin:<9.2f} | {ls_margin:<9.2f} | {ll_margin:<9.2f}")
          EOF
          ./venv/bin/python3 formal_audit.py
